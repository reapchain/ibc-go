syntax = "proto3";
package reapchain-core.libs.bits;

option go_package = "github.com/reapchain/reapchain-core/proto/reapchain-core/libs/bits";

message BitArray {
  int64           bits  = 1;
  repeated uint64 elems = 2;
}
